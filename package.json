{
  "private": true,
  "scripts": {
    "start": "lerna run start --stream --concurrency",
    "start-smm": "lerna run start-smm --stream --concurrency"
  },
  "devDependencies": {
    "lerna": "^2.8.0"
  },
  "dependencies": {
    "@angular/cli": "^7.0.3",
    "bootstrap": "^4.0.0"
  },
  "workspaces": [
    "packages/*",
    "ngx-dino/packages/*"
  ]
}
