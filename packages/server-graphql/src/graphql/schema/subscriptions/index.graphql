type Subscription {
  messageStream: AnyMessage

  runSelected: RunSelectedMessage
  runInitiated: RunInitiatedMessage
  runCompleted: RunCompletedMessage
  runCompletedRecords: [RunRecord]!
  runCompleted: [Run]!
}
