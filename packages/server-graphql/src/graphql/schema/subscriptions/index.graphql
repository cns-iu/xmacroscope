type Subscription {
  # Subscription for a list of runs
  runCompletedRecordsSubscription: [Run]!

  # Subscriptions for an individual run in various states
  runCompleted: MessageRun
  runInitiatedSubscription: MessageRun
  runSelectedSubscription: MessageRun
}
