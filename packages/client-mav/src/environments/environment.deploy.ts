export const environment = {
  production: true,
  useHashStrategy: true,
  projectConfiguration: {
    deploymentLocation: 'cosi',
    defaultUsState: 'OH',
    runTimout: 10000,
    endpoint: '/graphql'
  }
};
