export const environment = {
  production: true,
  useHashStrategy: true,
  projectConfiguration: {
    runTimout: 10000,
    deploymentLocation: 'cosi',
    defaultUsState: 'OH',
    endpoint: '/graphql'
  }
};
