export const environment = {
  production: true,
  useHashStrategy: true,
  projectConfiguration: {
    deploymentLocation: 'cns-iu',
    defaultUsState: 'IN',
    runTimout: 10000,
    endpoint: '/graphql'
  }
};
