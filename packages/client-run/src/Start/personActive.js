const personActive = runner => ({
  icon: runner.icon,
  color: runner.color,
  ageGroup: runner.ageGroup,
  favoriteActivity: runner.favoriteActivity,
  height: runner.height,
  zipCode: runner.zipCode,
  latitude: runner.latitude,
  longitude: runner.longitude,
  state: runner.state,
});

export default personActive;
