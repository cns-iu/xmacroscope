import styled from "styled-components";

const lgUI = `
font-family: "Roboto Mono",monospace;
font-weight: 700;
font-size: 400px;
`;
const TimerWrapper = styled.div`
  ${lgUI}
`;

export { TimerWrapper };
